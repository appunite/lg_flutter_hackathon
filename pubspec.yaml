name: lg_flutter_hackathon
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  audioplayers: ^6.0.0
  go_router: ^14.2.1
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  flutter_lints: ^4.0.0
  logging: ^1.2.0
  get_it: ^7.7.0
  gap: ^3.0.1
  overlay_tooltip: ^0.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/music/
    - assets/sfx/
